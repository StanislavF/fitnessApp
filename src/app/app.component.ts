import { Component } from '@angular/core';

@Component({
  selector: 'app-root',
  template: '<app-training-day></app-training-day>'
  //'<router-outlet></router-outlet>'
})
export class AppComponent {
  title = 'app';
}
